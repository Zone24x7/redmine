<% if ((User.current.admin? || !User.current.roles_for_project(@project).include?(Role.find_by_id(17))) && !(User.current.roles_for_project(@project).include?Role.find_by_id(27))) %>
  <div class="members box">
    <h3><%=l(:label_member_plural)%></h3>
    <% @users_by_role.keys.sort.each do |role| %>
      <p><span class="label"><%= role %>:</span> <%= @users_by_role[role].sort.collect{|u| link_to_user u}.join(", ").html_safe %></p>
    <% end %>
  </div>
<% end %>
